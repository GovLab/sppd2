# Ruby CircleCI 2.0 configuration file
#
# Check https://circleci.com/docs/2.0/language-ruby/ for more details
#
version: 2
jobs:
  build:
    docker:
      - image: circleci/ruby:2.4

    steps:
      - checkout

      - run: gem install bundler
      - run: bundle install
      - add-ssh-keys:
        fingerprints:
          - "0e:1d:2c:16:33:14:de:c4:2d:03:5d:5c:9f:cf:e3:77"
      - run: bash ./bin/automated.sh
